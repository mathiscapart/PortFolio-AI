# L'API tourne en process Windows natif, pas en conteneur : rien ne justifie
# encore un conteneur de plus en v0. Ce n'est PAS une contrainte de
# joignabilite d'Ollama, le service rag le joint deja depuis un conteneur
# via host.docker.internal. Decision reversible (etape 8).
# Le reste des dependances vient de backend/rag/requirements.txt.
fastapi==0.141.1
uvicorn==0.52.4
